<div align="center">
<img width="1200" height="475" alt="GHBanner" src="https://github.com/user-attachments/assets/0aa67016-6eaf-458a-adb2-6e31a0763ed6" />
</div>

# Run and deploy your AI Studio app

This contains everything you need to run your app locally.

View your app in AI Studio: https://ai.studio/apps/drive/1W6yEgbXPOE-IDhe-m1hjNTfbAgxROruK

## Run Locally

**Prerequisites:**  Node.js


1. Install dependencies:
   `npm install`
2. Set the `GEMINI_API_KEY` in [.env.local](.env.local) to your Gemini API key
3. Run the app:
   `npm run dev`

## Новые возможности для роли "Сотрудник"

Добавлен новый экран `EmployeeInputView` для пользователей с ролью "Сотрудник", который объединяет функционал ввода данных партии, параметров доски и выбора продукции на одном экране.

### Основной функционал:

1. **Единый экран ввода данных**:
   - Ввод параметров партии, длины, ширины и толщины доски в одну строку
   - Grid продукции с возможностью выбора изделий и указания количества
   - Сохранение данных на webhook через функцию `saveToGoogleSheet`

2. **Улучшения UX**:
   - Запоминание последней выбранной партии (localStorage)
   - Автоматический фокус на поле ширины после выбора партии с выделением существующего значения
   - Шаг ввода 10 для полей длины и ширины
   - Клик по изображению продукта для добавления в корзину (помимо кнопки "+")
   - Настройка количества колонок в grid продукции (по умолчанию 6 для desktop/tablet)

3. **Расчет KPI (коэффициента эффективности)**:
   - Расчет стоимости доски: `board_cost = unit_cost * boardVolumeM3`
   - Расчет общей стоимости продукции: `totalCost = sum(product.price * quantity)`
   - Расчет KPI: `KPI = totalCost / board_cost`
   - Отображение текущего KPI на кнопке "Сохранить" в реальном времени
   - Настраиваемое отображение эмодзи в модальном окне успеха в зависимости от KPI
   - Настраиваемая автоматическая отправка уведомления в Telegram при низком KPI

### Настройки KPI

Настройки KPI хранятся в формате JSON и могут быть заданы в двух местах:

1. **В листе "Настройки" (глобальные настройки по умолчанию)**:
   - Создайте запись с `key="bad_good_kpi"`
   - В поле `value` укажите JSON строку с настройками

2. **В листе "ПАРТИЯ" (настройки для конкретной партии)**:
   - Добавьте колонку `bad_good_kpi` в лист "ПАРТИЯ"
   - Для каждой партии можно указать индивидуальные настройки KPI в формате JSON строки
   - Если для партии настройки не указаны, используются настройки из листа "Настройки"
   - Если и там нет настроек, используются значения по умолчанию

**Формат JSON настройки KPI:**

```json
{
  "good": {
    "emoji": "🟢",
    "threshold": 1.5,
    "send": false,
    "condition": ">="
  },
  "ok": {
    "emoji": "🟡",
    "threshold": 1.2,
    "send": false,
    "condition": ">="
  },
  "bad": {
    "emoji": "🟠",
    "threshold": 1.0,
    "send": false,
    "condition": "<"
  },
  "very bad": {
    "emoji": "🔴",
    "threshold": 0.5,
    "send": true,
    "condition": "<"
  }
}
```

**Параметры настройки:**

- `emoji` - эмодзи, отображаемый в модальном окне успеха при достижении этого уровня KPI
- `threshold` - пороговое значение KPI для этого уровня
- `send` - флаг, определяющий, отправлять ли уведомление в Telegram при достижении этого уровня KPI
- `condition` - условие сравнения KPI с пороговым значением: `">="`, `"<="`, `">"`, `"<"`

**Логика проверки KPI:**

Приложение проверяет условия KPI в следующем порядке:
1. Если KPI соответствует условию `good` (по умолчанию KPI >= 1.5) → отображается emoji `good` и не отправляется уведомление
2. Если KPI соответствует условию `ok` (по умолчанию KPI >= 1.2) → отображается emoji `ok` и не отправляется уведомление
3. Если KPI соответствует условию `very bad` (по умолчанию KPI < 0.5) → отображается emoji `very bad` и отправляется уведомление в Telegram (если `send: true`)
4. Если KPI соответствует условию `bad` (по умолчанию KPI < 1.0) → отображается emoji `bad` и не отправляется уведомление
5. Если ни одно условие не выполняется (диапазон между порогами) → используется emoji `bad` по умолчанию

**Требования:**

- Настройки должны быть валидным JSON
- Все четыре уровня (`good`, `ok`, `bad`, `very bad`) должны быть указаны
- Пороговые значения должны быть числами
- Условия должны быть одной из допустимых операций сравнения
- Для отправки Telegram уведомлений должны быть настроены переменные окружения `VITE_TELEGRAM_BOT_TOKEN` и `VITE_TELEGRAM_CHAT_ID`

4. **Формат сохранения данных**:
   - Новый формат JSON для products: `{"название изделия": {"count": количество, "cost": стоимость}}`
   - Сохранение дополнительных полей: `unit_cost`, `board_cost`, `KPI`

**Пример сохранения данных:**

```json
{
  "unit_cost": 5000,
  "board_cost": 2500,
  "KPI": 1.8,
  "products": {
    "Рукоятка для топора": {
      "count": 5,
      "cost": 450
    },
    "Рукоятка для молотка": {
      "count": 3,
      "cost": 320
    }
  }
}
```

5. **Настройки окружения**:
   - `VITE_TELEGRAM_BOT_TOKEN` - токен Telegram бота для уведомлений
   - `VITE_TELEGRAM_CHAT_ID` - ID чата для отправки уведомлений

### Технические детали:

- Компонент использует React Hooks (`useState`, `useEffect`, `useRef`)
- Интеграция с Google Sheets через webhook
- Использование localStorage для персистентности данных
- Модальные окна для уведомлений (успешное сохранение, выбор партии)
- Адаптивный дизайн с использованием Tailwind CSS

## Progressive Web App (PWA)

Приложение поддерживает установку как PWA для работы офлайн и как нативное приложение.

### Возможности PWA:

- **Установка на устройство** - можно установить на домашний экран
- **Офлайн работа** - базовые функции работают без интернета
- **Кеширование** - статические ресурсы кешируются для быстрой загрузки
- **Обновления** - автоматическое обновление при изменении версии

### Настройка PWA:

1. **Генерация иконок** (см. [PWA-SETUP.md](PWA-SETUP.md)):
   - Откройте `public/generate-icons.html` в браузере
   - Сгенерируйте иконки 192x192 и 512x512
   - Сохраните как `icon-192x192.png` и `icon-512x512.png` в папку `public/`

2. **Проверка**:
   - Откройте DevTools (F12) → Application → Manifest
   - Убедитесь, что манифест загружается без ошибок

3. **Установка**:
   - В Chrome/Edge: используйте иконку установки в адресной строке
   - На Android: меню → "Добавить на главный экран"
   - На iOS: Safari → Поделиться → "На экран «Домой»"

Подробные инструкции смотрите в файле [PWA-SETUP.md](PWA-SETUP.md).
